<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.6.xsd">
    <changeSet id="create-user-project-table" author="Dmitrii">
        <sql>
            CREATE TABLE IF NOT EXISTS public.user_project
            (
                user_id  bigserial NOT NULL,
                project_id bigserial NOT NULL,
                CONSTRAINT user_project_pkey PRIMARY KEY (user_id, project_id),
                CONSTRAINT jjksdfdgsbtrywegnsbfjdswqiq FOREIGN KEY (user_id)
                REFERENCES public.users (user_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION,
                CONSTRAINT mnvzxsahubagpprtqlfvabauptq FOREIGN KEY (project_id)
                REFERENCES public.projects (project_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
                );
        </sql>
    </changeSet>
</databaseChangeLog>